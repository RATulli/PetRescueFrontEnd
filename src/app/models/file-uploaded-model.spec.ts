import { FileUploadedModel } from './file-uploaded-model';

describe('FileUploadedModel', () => {
  it('should create an instance', () => {
    expect(new FileUploadedModel()).toBeTruthy();
  });
});
